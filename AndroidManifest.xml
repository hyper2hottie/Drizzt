<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="realms.drizzt.mainactivity"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="14" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".DrizztActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity 
            android:name="realms.drizzt.newactivity.NewActivity"
            android:label="@string/newActivity" >
            	
        </activity>
        <activity 
            android:name="realms.drizzt.fragmentactivity.FragmentActivity"
            android:label="@string/FragmentActivity" >
            	
        </activity>
         <activity 
            android:name="realms.drizzt.drawcircle.CircleActivity"
            android:label="@string/CircleActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar" >
        </activity>
        <activity 
            android:name="realms.drizzt.movingcircle.MovingCircleActivity"
            android:label="@string/MovingCircle"
            android:theme="@android:style/Theme.Black.NoTitleBar" >
        </activity>
        <activity 
            android:name="realms.drizzt.bluetoothactivity.BluetoothActivity"
            android:label="@string/BluetoothActivity">                        
        </activity>
    </application>

</manifest>